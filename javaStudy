//@Target() 标注注解的注解，叫做元注解，Target注解用来修饰@component注解可以出现的位置
//ElementType.TYPE,ElementType.FIELD：@component注解可以出现在类上，可以出现在属性上
//@Target(value = {ElementType.TYPE,ElementType.FIELD})
//自定义注解
//使用某个注解的时候，如果注解的属性名是value的话，value可以省略
@Target({ElementType.TYPE,ElementType.FIELD})
//@Retention,这也是一个元注解，用来标注component注解最终保留在class文件中，并且可以被反射机制读取
@Retention(RetentionPolicy.RUNTIME)
public @interface component {

//可以在注解中定义属性
//String是属性类型，value是属性名
String value();

//String name();

//数组属性
//属性的类型是String[]
//属性名是names
//String[] names();

}

在其他地方使用component注解的时候也要实现这个注解中的属性
@component(属性名 = 属性值/value = "abc")
