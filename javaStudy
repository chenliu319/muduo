spring.xml
如果要扫描多个包,使用逗号隔开
<context:component-scan base-package="com.lin.spirng.bean,com.lin.spirng.dao"/>

//选择性实例化bean
//如果在某个包下有很多bean，有的bean标注了controller，有的bean标注了service，有的bean标注了dao，我想只对标注了controller的 bean进行实例化，其他的都不实例化怎么实现
//第一种解决方法
		use-default-filters = false:如果这个属性是false，表示扫描的包下面所有声明Bean的注解全部失效
<context:component-scan base-package="com.lin.spirng.bean,com.lin.spirng.dao" use-default-filters = "false">
	想让controller注解生效
	<context:include-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
</<context:component-scan>

第二种解决方法,默认use-default-filters = "true"
<context:component-scan base-package="com.lin.spirng.bean,com.lin.spirng.dao" use-default-filters = "true">
	想让controller注解失效
	<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
</<context:component-scan>
